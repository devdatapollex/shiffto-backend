info:
  name: Get Trips
  type: http
  seq: 2

http:
  method: GET
  url: "{{localurl}}/api/v1/trips"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

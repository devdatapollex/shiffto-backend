info:
  name: Get Available Shipments
  type: http
  seq: 9

http:
  method: GET
  url: "{{localurl}}/api/v1/trips/available-shipments"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

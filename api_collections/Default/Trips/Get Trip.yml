info:
  name: Get Trip
  type: http
  seq: 3

http:
  method: GET
  url: "{{localurl}}/api/v1/trips/{{tripId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

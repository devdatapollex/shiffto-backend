info:
  name: Delete ShipmentCategory
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{localurl}}/api/v1/shipment-categories/{{categoryId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

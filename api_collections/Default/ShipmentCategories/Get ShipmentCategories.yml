info:
  name: Get ShipmentCategories
  type: http
  seq: 2

http:
  method: GET
  url: "{{localurl}}/api/v1/shipment-categories"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

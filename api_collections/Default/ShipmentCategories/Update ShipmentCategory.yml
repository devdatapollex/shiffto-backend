info:
  name: Update ShipmentCategory
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{localurl}}/api/v1/shipment-categories/{{categoryId}}"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "maxWeight": 60
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

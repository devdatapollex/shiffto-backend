info:
  name: Get ShipmentCategory
  type: http
  seq: 3

http:
  method: GET
  url: "{{localurl}}/api/v1/shipment-categories/{{categoryId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

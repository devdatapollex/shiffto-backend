info:
  name: Get Step Definitions
  type: http
  seq: 1

http:
  method: GET
  url: "{{localurl}}/api/v1/shipments-steps"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

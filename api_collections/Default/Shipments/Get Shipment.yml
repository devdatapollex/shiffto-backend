info:
  name: Get Shipment
  type: http
  seq: 4

http:
  method: GET
  url: "{{localurl}}/api/v1/shipments/{{shipmentId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Get Shipment Steps
  type: http
  seq: 10

http:
  method: GET
  url: "{{localurl}}/api/v1/shipments/{{shipmentId}}/steps"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

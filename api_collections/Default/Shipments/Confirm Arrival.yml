info:
  name: Confirm Arrival
  type: http
  seq: 13

http:
  method: POST
  url: "{{localurl}}/api/v1/shipments/{{shipmentId}}/steps/confirm-arrival"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "notes": "Package arrived at destination airport"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Send Delivery OTP
  type: http
  seq: 7

http:
  method: POST
  url: "{{localurl}}/api/v1/shipments/{{shipmentId}}/send-delivery-otp"
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

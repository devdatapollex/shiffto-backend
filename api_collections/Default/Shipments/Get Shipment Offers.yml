info:
  name: Get Shipment Offers
  type: http
  seq: 12

http:
  method: GET
  url: "{{localurl}}/api/v1/shipments/{{shipmentId}}/offers"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

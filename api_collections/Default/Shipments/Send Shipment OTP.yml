info:
  name: Send Shipment OTP
  type: http
  seq: 9

http:
  method: POST
  url: "{{localurl}}/api/v1/shipments/send-otp"
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Delete Shipment
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{localurl}}/api/v1/shipments/{{shipmentId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Get Shipments
  type: http
  seq: 2

http:
  method: GET
  url: "{{localurl}}/api/v1/shipments?type=available"
  params:
    - name: type
      value: available
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Get Shipment Details
  type: http
  seq: 11

http:
  method: GET
  url: "{{localurl}}/api/v1/shipments/{{shipmentId}}/details"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Get Received Offers
  type: http
  seq: 3

http:
  method: GET
  url: "{{localurl}}/api/v1/offers/received"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

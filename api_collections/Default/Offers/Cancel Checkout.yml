info:
  name: Cancel Checkout
  type: http
  seq: 6

http:
  method: POST
  url: "{{localurl}}/api/v1/offers/{{offerId}}/cancel-checkout"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

name: Reject Withdrawal
type: http
seq: 5

request:
  url: "{{baseUrl}}/api/v1/withdrawals/admin/wdr_id_here/reject"
  method: PATCH
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "rejectionReason": "Invalid bKash account details"
      }

auth:
  type: bearer
  token: "{{token}}"

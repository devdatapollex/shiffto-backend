info:
  name: Get Sender Payments Summary
  type: http
  seq: 1

http:
  method: GET
  url: "{{localurl}}/api/v1/payments/sender-summary"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Get Notifications
  type: http
  seq: 1

http:
  method: GET
  url: "{{localurl}}/api/v1/notifications"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Get Session
  type: http
  seq: 4

http:
  method: GET
  url: "{{localurl}}/api/auth/get-session"
  headers:
    - name: Origin
      value: "{{frontendurl}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

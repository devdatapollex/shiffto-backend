info:
  name: Get Profile
  type: http
  seq: 1

http:
  method: GET
  url: "{{localurl}}/api/v1/profile"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Deactivate Account
  type: http
  seq: 5

http:
  method: POST
  url: "{{localurl}}/api/v1/profile/deactivate"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
